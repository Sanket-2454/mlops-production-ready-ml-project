# Minimal pyproject.toml to enable PEP 517 / PEP 660 editable installs
# See: https://peps.python.org/pep-0518/ and https://peps.python.org/pep-0660/

[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"
